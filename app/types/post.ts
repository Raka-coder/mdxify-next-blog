export interface Post {
  slug: string;
  title: string;
  date: string;
  thumbnail: string;
  content: string;
}

export interface PostMetadata {
  title: string;
  date: string;
  thumbnail: string;
  slug: string;
}
